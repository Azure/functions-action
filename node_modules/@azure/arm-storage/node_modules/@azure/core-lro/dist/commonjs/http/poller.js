var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var poller_exports = {};
__export(poller_exports, {
  createHttpPoller: () => createHttpPoller
});
module.exports = __toCommonJS(poller_exports);
var import_operation = require("./operation.js");
var import_poller = require("../poller/poller.js");
var import_utils = require("./utils.js");
function createHttpPoller(lro, options) {
  const {
    resourceLocationConfig,
    intervalInMs,
    processResult,
    restoreFrom,
    updateState,
    withOperationLocation,
    resolveOnUnsuccessful = false,
    baseUrl,
    skipFinalGet
  } = options || {};
  return (0, import_poller.buildCreatePoller)({
    getStatusFromInitialResponse: import_operation.getStatusFromInitialResponse,
    getStatusFromPollResponse: import_operation.getOperationStatus,
    isOperationError: import_operation.isOperationError,
    getOperationLocation: import_operation.getOperationLocation,
    getResourceLocation: import_operation.getResourceLocation,
    getPollingInterval: import_operation.parseRetryAfter,
    getError: import_operation.getErrorFromResponse,
    resolveOnUnsuccessful
  })(
    {
      init: async () => {
        const response = await lro.sendInitialRequest();
        const config = (0, import_operation.inferLroMode)(response.rawResponse, resourceLocationConfig, skipFinalGet);
        return {
          response,
          operationLocation: (0, import_utils.rewriteUrl)({ url: config?.operationLocation, baseUrl }),
          resourceLocation: (0, import_utils.rewriteUrl)({ url: config?.resourceLocation, baseUrl }),
          initialRequestUrl: config?.initialRequestUrl,
          requestMethod: config?.requestMethod,
          ...config?.mode ? { metadata: { mode: config.mode } } : {}
        };
      },
      poll: lro.sendPollRequest
    },
    {
      intervalInMs,
      withOperationLocation,
      restoreFrom,
      updateState,
      processResult: processResult ? ({ flatResponse }, state) => processResult(flatResponse, state) : ({ flatResponse }) => flatResponse
    }
  );
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  createHttpPoller
});
//# sourceMappingURL=poller.js.map
